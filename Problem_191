int hammingWeight(int n) {
    int count = 0;
    while(n!=0){
        //checking last bit
        if (n&1){ // & is use for the camparison in the bit
            count++;
        }
        n = n>>1; // yeh shift karne ke liye use kiya hai
    }
    return count;
}
